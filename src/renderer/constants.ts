/**
 * レンダラー共通の定数定義
 */

// === Layout ===

/** メインウィンドウのタイトルバー（MenuBar）の高さ（px） */
export const TITLE_BAR_HEIGHT = 56;

// === Dimension Line ===

/** 寸法線として認識する最小距離（px） */
export const MIN_DIMENSION_LINE_DISTANCE = 2;

// === Anchor / Overlay Controls ===

/** アンカーポイントの半径（px） */
export const ANCHOR_RADIUS = 15;

/** アンカーポイントの枠線色 */
export const ANCHOR_STROKE_COLOR = "#1919eb";

/** アンカーポイントの塗りつぶし色 */
export const ANCHOR_FILL_COLOR = "#4e4eff";

/** オーバーレイ枠線の色 */
export const OVERLAY_STROKE_COLOR = "#4e4eff";

/** オーバーレイ枠線の幅（px） */
export const OVERLAY_STROKE_WIDTH = 3;
