export const STARTUP_OPTION_TOKENS = new Set([
    "--scene",
    "--images",
    "--opacity",
    "--position",
    "--size",
    "--always-on-top",
    "--click-through",
    "--fullscreen",
    "--silent",
    "--minimize",
]);

export const CONTROL_COMMAND_OPTION_TOKENS = new Set([
    "--add-image",
    "--set-opacity",
    "--switch-scene",
    "--export",
]);
